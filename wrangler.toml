name = "yt-auto-screenshot"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 绑定浏览器渲染服务
[browser]
binding = "BROWSER"

# 绑定 R2 存储桶，用于保存图片
[[r2_buckets]]
binding = "MY_BUCKET"
bucket_name = "yt-screenshots"

# (可选) 定时触发器：每 30 分钟自动截图一次
[triggers]
crons = ["*/30 * * * *"]
